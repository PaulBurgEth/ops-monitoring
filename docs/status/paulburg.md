# paulburg.com — status

- **Repo:** PaulBurgEth/paulburg-com (Next.js/MDX)
- **Live:** https://paulburg.com (Cloudflare → Vercel, project `paulburg-com`)
- **CI:** ❌ no workflows yet (report shows "no CI runs")
- **Sentry:** ✅ project `paulburg` + alert rules — SDK install pending (PR)
- **Deploy:** Vercel `paulburg-com` (needs `VERCEL_TOKEN`)
- **Uptime:** ✅ HTTP 200; secret scanning already enabled
- **Dependabot:** ❌ to add `dependabot.yml` (npm) PR
- **Next:** PR — `@sentry/nextjs` + `dependabot.yml`. Optional: minimal CI (lint/build) so CI status is meaningful.
